/**
 * Curated autonomous sprites from `map-sprites-01`.
 *
 * This first pass focuses on tree-like standalone objects (trees, dead trees,
 * saplings) and intentionally skips assembly-only atlas fragments.
 */

export * from './Trees.js';
