# Engine tools

Small utility helpers that support the engine runtime or debugging workflows
without belonging to the map simulation core.

- GameConsole.js: lightweight DOM log panel that appends entries and can auto-scroll to the latest message.
